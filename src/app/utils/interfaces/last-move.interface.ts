/**
 * @typedef LastMove
 *
 * Represents the coordinates of the last move made on the game board.
 *
 * @property row The row index of the move (0-based)
 * @property column The column index of the move (0-based)
 */
export interface LastMove { row: number; column: number }
